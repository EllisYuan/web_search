# web_search

## Agent skills

### Issue tracker

Issues 追踪在本仓库的 GitHub Issues（`EllisYuan/web_search`），通过 `gh` CLI 操作。见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个 canonical 角色的默认 label 字符串，未做重命名。见 `docs/agents/triage-labels.md`。

### Domain docs

Single-context 布局：根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
